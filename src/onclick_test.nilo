
flow {
    start: Clicker
}

timeline Clicker {
    let count: Number = 0
    
    Text("counter: {}", count)
    Button(id: increment_btn, label: "increment")
    when user.click(increment_btn) {
        set count = count + 1
    }
}
