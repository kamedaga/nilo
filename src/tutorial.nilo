flow {
    start: main
    start -> [main]
    main -> [start]
}

timeline start {
    VStack(style: {
        background: "#282c34",
        padding: 20px,
        align: "center",
        min_height: "100vh",
        width: "100vw"
    }) {
        Spacing(20)

        // タイトル
        Text("Nilo基本仕様デモ", style: {
            font_size: 32px,
            color: "#61dafb",
            font_weight: "bold"
        })
7
        Spacing(30)

        // Text要素のデモ
        VStack(style: {
            background: "#3c4043",
            padding: 15px,
            rounded: 8px,
            max_width: 600px,
            margin: 10px
        }) {
            Text("1. Text要素", style: {
                font_size: 20px,
                color: "#61dafb",
                font_weight: "bold"
            })
            Text("通常のテキスト", style: {
                color: "#ffffff",
                font_size: 16px
            })
            Text("スタイル付きテキスト", style: {
                color: "#ff6b6b",
                font_size: 18px,
                font_weight: "bold"
            })
        }

        Spacing(20)

        // Button要素のデ��
        VStack(style: {
            background: "#3c4043",
            padding: 15px,
            rounded: 8px,
            max_width: 600px,
            margin: 10px
        }) {
            Text("2. Button要素", style: {
                font_size: 20px,
                color: "#61dafb",
                font_weight: "bold"
            })

            HStack(style: {
                gap: 10px,
                margin_top: 10px
            }) {
                Button(id: primary_btn, label: "プライマリ", style: {
                    background: "#61dafb",
                    color: "#282c34",
                    padding: "10px 20px",
                    rounded: 6px,
                    font_weight: "bold"
                })

                Button(id: secondary_btn, label: "セカンダリ", style: {
                    background: "transparent",
                    color: "#61dafb",
                    padding: "10px 20px",
                    rounded: 6px,
                    border: "2px solid #61dafb"
                })
            }
        }

        Spacing(20)

        // TextInput要素のデモ
        VStack(style: {
            background: "#3c4043",
            padding: 15px,
            rounded: 8px,
            max_width: 600px,
            margin: 10px
        }) {
            Text("3. TextInput要素", style: {
                font_size: 20px,
                color: "#61dafb",
                font_weight: "bold"
            })

            VStack(style: {
                gap: 10px,
                margin_top: 10px,
                align: "start",
                width: "100%"
            }) {
                Text("名前:", style: {
                    color: "#ffffff",
                    font_size: 14px
                })
                TextInput("", style: {
                    background: "#ffffff",
                    color: "#000000",
                    padding: 8px,
                    rounded: 4px,
                    width: "100%",
                    font_size: 16px
                })

                Text("メッセージ:", style: {
                    color: "#ffffff",
                    font_size: 14px,
                    margin_top: 10px
                })
                TextInput("", style: {
                    background: "#ffffff",
                    color: "#000000",
                    padding: 8px,
                    rounded: 4px,
                    width: "100%",
                    height: 80px,
                    font_size: 16px
                })
            }
        }

        Spacing(20)

        // レイアウトのデモ
        VStack(style: {
            background: "#3c4043",
            padding: 15px,
            rounded: 8px,
            max_width: 600px,
            margin: 10px
        }) {
            Text("4. レイアウト（VStack & HStack）", style: {
                font_size: 20px,
                color: "#61dafb",
                font_weight: "bold"
            })

            Text("HStack（水平レイアウト）:", style: {
                color: "#ffffff",
                font_size: 14px,
                margin_top: 10px
            })

            HStack(style: {
                gap: 10px,
                margin_top: 5px
            }) {
                VStack(style: {
                    background: "#ff6b6b",
                    padding: 10px,
                    rounded: 4px,
                    width: 80px,
                    height: 60px,
                    align: "center"
                }) {
                    Text("Box 1", style: {
                        color: "#ffffff",
                        font_size: 12px
                    })
                }

                VStack(style: {
                    background: "#4ecdc4",
                    padding: 10px,
                    rounded: 4px,
                    width: 80px,
                    height: 60px,
                    align: "center"
                }) {
                    Text("Box 2", style: {
                        color: "#ffffff",
                        font_size: 12px
                    })
                }

                VStack(style: {
                    background: "#45b7d1",
                    padding: 10px,
                    rounded: 4px,
                    width: 80px,
                    height: 60px,
                    align: "center"
                }) {
                    Text("Box 3", style: {
                        color: "#ffffff",
                        font_size: 12px
                    })
                }
            }
        }

        Spacing(20)

        // スタイルシステムのデモ
        VStack(style: {
            background: "#3c4043",
            padding: 15px,
            rounded: 8px,
            max_width: 600px,
            margin: 10px
        }) {
            Text("5. スタイルシステム", style: {
                font_size: 20px,
                color: "#61dafb",
                font_weight: "bold"
            })

            Text("色とサイズ:", style: {
                color: "#ffffff",
                font_size: 14px,
                margin_top: 10px
            })

            VStack(style: {
                gap: 5px,
                margin_top: 5px
            }) {
                Text("16px 通常", style: { color: "#ffffff", font_size: 16px })
                Text("20px 太字", style: { color: "#61dafb", font_size: 20px, font_weight: "bold" })
                Text("背景付きテキスト", style: {
                    color: "#ffffff",
                    background: "#ff6b6b",
                    padding: 5px,
                    rounded: 4px
                })
            }
        }

        Spacing(30)

        Button(id: next_btn, label: "戻る →", style: {
            background: "#61dafb",
            color: "#282c34",
            padding: "12px 24px",
            font_size: 16px,
            font_weight: "bold",
            rounded: 6px
        })

        Spacing(20)
    }

    when user.click(next_btn) {
        navigate_to(main)
    }
}

timeline main {
    VStack(style: {
        background: "#282c34",
        padding: 20px,
        align: "center",
        min_height: "100vh",
        width: "100vw",
        rounded: 4px
    }) {
        Spacing(60)

        // Niloロゴ（アイコン風）
        Text("⚡", style: {
            font_size: 80px,
            color: "#61dafb",
            padding: 5px
        })

        Spacing(20)

        Text("Welcome to Nilo", style: {
            color: "#61dafb",
            font_size: 48px,
            font_weight: "bold",
            right: 10px
        })

        Text("{}", state.name, style: {
            color: "#ffffff",
            font_size: 24px,
            margin_top: 10px
        })

        Spacing(10)

        Text("A modern UI framework for building fast applications", style: {
            color: "#ffffff",
            font_size: 18px,
            text_align: "center",
            max_width: 400px
        })

        Spacing(40)

        HStack(style: {
            gap: 20px
        }) {
            Button(
                id: learn_btn,
                label: "Get Started",
                style: {
                    background: "#61dafb",
                    color: "#282c34",
                    padding: "14px 28px",
                    rounded: 8px,
                    font_weight: "bold",
                    border: "none",
                    font_size: 16px
                }
            )

            Button(
                id: docs_btn,
                label: "Learn More",
                style: {
                    background: "transparent",
                    color: "#61dafb",
                    padding: "14px 28px",
                    rounded: 8px,
                    border: "2px solid #61dafb",
                    font_size: 16px
                }
            )
        }

        Spacing(60)

        // 機能紹介セクション
        VStack(style: {
            gap: 30px,
            max_width: 600px
        }) {
            HStack(style: {
                gap: 20px,
                align: "center"
            }) {
                Text("🚀", style: {
                    font_size: 24px
                })
                VStack(style: {
                    align: "start"
                }) {
                    Text("Fast Development", style: {
                        color: "#ffffff",
                        font_size: 18px,
                        font_weight: "bold"
                    })
                    Text("Hot reload and instant feedback", style: {
                        color: "#aaaaaa",
                        font_size: 14px
                    })
                }
            }

            HStack(style: {
                gap: 20px,
                align: "center"
            }) {
                Text("⚡", style: {
                    font_size: 24px
                })
                VStack(style: {
                    align: "start"
                }) {
                    Text("High Performance", style: {
                        color: "#ffffff",
                        font_size: 18px,
                        font_weight: "bold"
                    })
                    Text("Built with Rust for maximum speed", style: {
                        color: "#aaaaaa",
                        font_size: 14px
                    })
                }
            }

            HStack(style: {
                gap: 20px,
                align: "center"
            }) {
                Text("🎨", style: {
                    font_size: 24px
                })
                VStack(style: {
                    align: "start"
                }) {
                    Text("Beautiful UI", style: {
                        color: "#ffffff",
                        font_size: 18px,
                        font_weight: "bold"
                    })
                    Text("Modern design system included", style: {
                        color: "#aaaaaa",
                        font_size: 14px
                    })
                }
            }
        }

        Spacing(60)

        Text("Edit src/app.nilo to get started", style: {
            color: "#1E93AB",
            font_size: 16px,
            font_family: "monospace",
            background: "#1e1e1e",
            padding: "8px 16px",
            rounded: 6px,
            border: "1px solid #444"
        })

        Spacing(40)

        Text("")
    }

    when user.click(learn_btn) {
        navigate_to(start)
    }
}
